# Razorpay Public Key ID — safe to expose on the frontend (NOT the secret)
# Must match the RAZORPAY_KEY_ID in server/.env
VITE_RAZORPAY_KEY_ID=rzp_test_T0DYTL93FZxmsL

# URL of the backend payment server
VITE_BACKEND_URL=http://localhost:5000
